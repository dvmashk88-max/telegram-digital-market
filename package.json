{
  "name": "max-digital-market",
  "version": "1.2.0",
  "description": "MAX Digital Market storefront",
  "keywords": [
    "example"
  ],
  "license": "MIT",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "npm ci --prefix examples/react --include=dev && npm run build --prefix examples/react",
    "check:violet-pricing-all": "node scripts/check-violet-pricing-all.mjs",
    "check:violet-ru-pricing": "node scripts/check-violet-ru-pricing.mjs",
    "db:migrate": "node examples/backend-node/scripts/migrate.mjs",
    "max:set-webhook": "node scripts/set-max-webhook.mjs",
    "start": "NODE_EXTRA_CA_CERTS=certs/russian-trusted-ca.pem STATIC_DIR=examples/react/dist node examples/backend-node/server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  }
}

{
  "name": "max-digital-market",
  "version": "1.2.0",
  "description": "MAX Digital Market storefront",
  "keywords": [
    "example"
  ],
  "license": "MIT",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "build": "npm ci --prefix examples/react && npm run build --prefix examples/react",
    "check:violet-pricing-all": "node scripts/check-violet-pricing-all.mjs",
    "check:violet-ru-pricing": "node scripts/check-violet-ru-pricing.mjs",
    "max:set-webhook": "node scripts/set-max-webhook.mjs",
    "start": "STATIC_DIR=examples/react/dist node examples/backend-node/server.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
